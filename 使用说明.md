# Unitree H1 ROS 2 仿真使用说明

ROS 2 Humble + Ignition Gazebo (Fortress) 下的 Unitree H1 人形机器人仿真。
本说明覆盖：构建、启动、**如何让 H1 在 Gazebo 里站起来并移动**、参数、排错。

---

## 1. 环境要求

- Ubuntu 22.04 + ROS 2 Humble
- Ignition Gazebo Fortress：`sudo apt install ros-humble-ros-gz`
- 依赖包：`ros_gz_sim`、`ros_gz_bridge`、`ros_gz_interfaces`、`robot_state_publisher`、`rviz2`
- **WSL2 用户**：本机 OpenGL 是 D3D12/Mesa（受限），launch 已强制用 ogre v1 渲染引擎，无需手动设置。

---

## 2. 构建

```bash
./ros2_launch.sh build
```

`ros2_launch.sh` 把所有命令包在一个**干净环境**里运行（`env -i` + 强制 FastRTPS），
用来避开 conda 抢占 ROS 的 Python、以及 CycloneDDS 网卡报错。**不要绕过它直接用裸 `colcon`/`ros2`**，
除非你自己复刻了同样的环境。

> 工作区用 `--symlink-install`。改 Python 脚本、launch、URDF **无需重新构建**（软链接即时生效）；
> 只有新增可执行脚本或改 `CMakeLists.txt` 时才要再 `build` 一次。

---

## 3. 启动

| 命令 | 说明 |
|---|---|
| `./ros2_launch.sh full` | **推荐**。Gazebo + H1 + 运动学底座驱动。机器人**直立站立**，可被 `/cmd_vel` 驱动在世界里移动 |
| `./ros2_launch.sh gazebo` | 只启动仿真本身（**无底座驱动 → 机器人会瘫倒**，仅用于看场景/调 URDF） |
| `./ros2_launch.sh teleop` | 键盘遥控（另开一个终端） |
| `./ros2_launch.sh gazebo --no-rviz` | 不开 RViz |

### 典型用法（两个终端）

```bash
# 终端 1：起仿真，机器人站好
./ros2_launch.sh full

# 终端 2：键盘遥控
./ros2_launch.sh teleop
```

键盘：`W/S` 前后、`A/D` 左右平移、`Q/E` 左右转、`空格` 急停、`Z/X`·`C/V` 调速、`ESC` 退出。

> 本运动学底座下，方向键**直接驱动底座移动**——不需要先按 `3` 切 WALK 模式。

---

## 4. H1 是怎么在 Gazebo 里"站起来"并移动的（原理）

**背景**：这套 SDK 没有平衡/运动控制器。一个物理上自由的双足机器人是倒立摆，
没有踝/髋平衡反馈，仿真一开物理就会立刻倾倒。

**方案：运动学浮动底座**（`src/h1_control/scripts/kinematic_base_driver.py`）

1. 订阅 `/cmd_vel`，按**机体坐标**积分出底座位姿 `(x, y, yaw)`，高度 `z` 固定、`roll=pitch=0`；
2. 每 20 ms（50 Hz）通过 Gazebo 的 `set_pose` 服务**硬设定骨盆位姿**——位置和朝向都被强制，
   所以机器人**永远直立、高度恒定**，按指令平滑滑行/转向，**绝不会倒**；
3. **行走动画**：同一节点按速度生成正弦步态，把关节目标发到每个关节的
   `JointPositionController` 插件——腿前后摆动、摆动相屈膝抬脚、手臂反向摆动，
   步频随速度提高。静止时平滑回到站姿（`<initial_position>`）。这是**视觉动画**
   （脚仍会滑动），不是真实动力学行走。

数据流：

```
keyboard_teleop ─/cmd_vel─> kinematic_base_driver ─┬─ set_pose 服务 ──> Gazebo 骨盆位姿(直立滑行)
                                                   └─ 19×/…/cmd_pos ──> JointPositionController(腿/臂摆动)
                              （服务/话题均由 ros_gz_bridge 桥接）
```

实测结果（`full` 启动，发 `vx=0.4`）：base_x 稳定前进、`z≈0.98`、`roll/pitch≈0`
全程直立不倒，同时 left_knee 在 0.79（站立相）↔1.27（摆动屈膝）之间循环 → 走路摆动正常。

---

## 5. 可调参数

`simulation_control.launch.py` / `kinematic_base_driver.py`：

| 参数 | 默认 | 说明 |
|---|---|---|
| `stand_height` | `0.98` | 骨盆保持的高度（m）。腿太陷/悬空就调它 |
| `update_rate` | `50.0` | 底座位姿刷新频率（Hz） |
| `max_vx / max_vy / max_wz` | `1.0 / 0.5 / 1.5` | 速度上限 |
| `animate_gait` | `True` | 是否做行走摆动动画（设 `False` 则腿保持站姿） |

例：

```bash
ros2 launch h1_control simulation_control.launch.py stand_height:=0.95 rviz:=false
```

---

## 6. 话题 / 服务

| 名称 | 类型 | 方向 |
|---|---|---|
| `/cmd_vel` | `geometry_msgs/Twist` | 遥控输入 → 底座驱动 |
| `/world/h1_world/set_pose` | `ros_gz_interfaces/srv/SetEntityPose` | 底座驱动 → Gazebo（由 `set_pose_bridge` 桥接） |
| `/model/h1/joint/<关节>/cmd_pos` (×19) | `std_msgs/Float64` | 步态目标 → 关节控制器（由 `joint_cmd_bridge` 桥接） |
| `/joint_states` `/odom` `/imu` `/clock` | — | Gazebo → ROS（`ros_gz_bridge`） |

---

## 7. 已知问题 & 排错

- **teleop 报 `No executable found`**：脚本缺可执行位。已 `chmod +x src/h1_control/scripts/*.py`；
  若重新 clone 后复发，再执行一次（建议把权限提交进 git）。
- **Gazebo 崩 `Ogre::UnimplementedException ... GL3PlusTextureGpu::copyTo`**：WSL2 的 OpenGL 受限。
  已在 `h1_gazebo.launch.py` 用 `--render-engine ogre`（v1）解决。换到带 GPU 直通的机器想要更好画质，
  可去掉该参数恢复 ogre2。
- **机器人一开就瘫倒**：你用的是 `./ros2_launch.sh gazebo`（没有底座驱动）。请改用 `./ros2_launch.sh full`。
- **日志里 `joint_state_publisher ... process has died`**：`h1_gazebo.launch.py` 里这个节点是冗余的
  （关节状态已由 URDF 的 gz JointStatePublisher 插件 + 桥提供），**报错无害**，可忽略或从该 launch 删除。

---

## 8. 局限与后续

- 这是**运动学可视化**：底座由 `set_pose` 驱动滑行，腿做随速度的摆动动画。
  脚与地面**没有真实约束**（会滑动），步态是观感动画而非物理行走。
- 要做到物理上**自由站立/行走**，需要实现真正的平衡 + 全身运动控制器（如 MPC/WBC），
  超出当前简化 SDK 的范围。
